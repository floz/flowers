var src = "./src/"

module.exports = {
  paths: {
    build: "./build/",
    dist: "./dist/",
    static: "./static/",

    src: src,

    views: src + "views/",
    scripts: src + "scripts/",
    styles: src + "styles/"
  }
}
